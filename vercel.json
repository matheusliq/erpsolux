{
    "version": 2,
    "installCommand": "npm install --no-package-lock",
    "buildCommand": "prisma generate && next build",
    "framework": "nextjs",
    "regions": [
        "gru1"
    ],
    "env": {
        "NODE_ENV": "production"
    }
}